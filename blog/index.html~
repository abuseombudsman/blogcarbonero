---
layout: DefaultSinCarouselblog
title: Blog Carbonero
name: blog
paginate: true
tags: "Blog carbonero, blog aurinegro, blog de peñarol"
autor: CAPeñarol
---

<h1 style="font-family:monospace;color:#fff;background:blue;" class="rounded"><i class="fa fa-star" style="font-size:1.0em;"></i>Blog Carbonero</h1> 

{% for post in paginator.posts %}
{% if post.category != noticias %}

<header class="post-header">
   	   <h4 itemprop="name headline"><a href="{{ post.url }}" style="color:#d9deb0;">{{ post.title | escape }}</a></h4>
   	   <p class="post-meta">
      	      <time datetime="{{ page.date | date_to_xmlschema }}" itemprop="datePublished" style="color:#fff;font-size:0.9em;margin-right:10px;">
              	    {% assign date_format = site.minima.date_format | default: "%b %-d, %Y" %}
                       <i class="glyphicon glyphicon-calendar" style="color:#fd5206;"><span> </span></i>
        	    {{ post.date | date: date_format }}
      	      </time>
      	      {% if post.author %}
              	 <i class="glyphicon glyphicon-user" style="color:#fd5206;font-size:0.9em;"><span> </span></i> <span itemprop="author" itemscope itemtype="http://schema.org/Person"><span itemprop="name" style="font-size:0.9em;margin-right:10px;">{{ post.author }}</span></span>
      	      {% endif %}
              {% if post.tiempo_de_lectura %}
      	      	 <i class="glyphicon glyphicon-time" style="color:#fd5206;font-size:0.9em;"></i><span> </span> <span style="font-size:0.9em;"> lectura:
		 {{ post.tiempo_de_lectura }}</span>
              {% endif %}
          </p>
  </header>


<div class="row-fluid">
  <ul class="thumbnails">
    <li class="span12">
      <div class="thumbnail">
        <center>
	
	  <img data-src="holder.js/300x200" class="image" alt="" src="{{ post.image1 }}" width="420">
	  
	</center>
        <div class="caption">
          <h4 style="color:#d9deb0;">{{ post.title }}</h4>
	    <p>
	      {{ post.description }}
            </p>
	    <p>
	      <a href="{{ site.url }}/{{ post.url }}" class="btn btn-primary">Leer más
	      </a>
	    </p>
	</div>
      </div>
    </li>
  </ul>
</div>
{% endif %}
{% endfor %}

<!-- Pagination links -->
<div class="pagination">
  {% if paginator.previous_page %}
    <a href="{{ paginator.previous_page_path }}" class="previous">Previous</a>
  {% else %}
    <span class="previous">Previous</span>
  {% endif %}
  <span class="page_number ">Page: {{ paginator.page }} of {{ paginator.total_pages }}</span>
  {% if paginator.next_page %}
    <a href="{{ paginator.next_page_path }}" class="next">Next</a>
  {% else %}
    <span class="next ">Next</span>
  {% endif %}
</div>
